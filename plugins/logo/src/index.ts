// @hortus Logo — component-only plugin entry point.
// Component-only plugins are loaded via side-effect import; the actual
// component is exposed through the "./components" subpath export.
export * from "./components"
