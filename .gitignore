.DS_Store
.gitignore
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix
.quartz/
quartz/cli/tui/dist/

# @hortus local plugin build artifacts (Quartz rebuilds these on install).
# Remove this line if you prefer to commit prebuilt dist/ for faster installs.
plugins/*/dist/
